# 🚀 Инструкция по деплою на сервер через SSH

## Быстрый старт

### Вариант 1: Полный деплой (рекомендуется)
```bash
./deploy_full.sh
```

Этот скрипт:
- ✅ Создает все необходимые директории на сервере
- ✅ Синхронизирует все backend файлы
- ✅ Синхронизирует все frontend файлы (включая критически важный `lib/api.ts`)
- ✅ Синхронизирует конфигурационные файлы
- ✅ Останавливает старые контейнеры
- ✅ Создает/проверяет администратора
- ✅ Собирает и запускает приложение
- ✅ Проверяет работоспособность

### Вариант 2: Быстрый деплой (только измененные файлы)
```bash
./deploy_to_server.sh
```

## Требования

### На локальной машине:
- `sshpass` - для автоматической аутентификации SSH
  ```bash
  # macOS
  brew install hudochenkov/sshpass/sshpass
  
  # Linux
  sudo apt-get install sshpass
  ```

### На сервере:
- Docker и Docker Compose
- Python 3.8+
- Доступ по SSH

## Настройка

Перед первым запуском отредактируйте скрипты и укажите:
- `SERVER` - адрес сервера (например: `root@185.247.17.188`)
- `PASSWORD` - пароль для SSH
- `SERVER_PATH` - путь на сервере (например: `/root/agb_passports`)
- `LOCAL_PATH` - локальный путь к проекту

## Что синхронизируется

### Backend:
- Все API endpoints (`passports.py`, `nomenclature.py`, `users.py`, `templates.py`)
- Основные файлы (`main.py`, `database.py`, `models.py`, `auth.py`)
- Утилиты (`pdf_generator.py`, `sticker_template_generator.py`, `create_logo.py`)
- Requirements (`requirements.txt`)

### Frontend:
- **lib/api.ts** (критически важно!)
- Все компоненты (`MainApp.tsx`, `ProtectedRoute.tsx`, и др.)
- Все хуки (`usePassports.ts`, `useNomenclature.ts`, `useUsers.ts`)
- Контексты (`AuthContext.tsx`)
- Страницы (`login.tsx`, `index.tsx`, `_app.tsx`)
- Конфигурация (`next.config.js`, `package.json`)

### Конфигурация:
- `docker-compose.yml`
- `Dockerfile.backend`
- `Dockerfile.frontend`
- `nginx.production.conf`
- Скрипты запуска и проверки

## Процесс деплоя

1. **Создание директорий** - создаются все необходимые директории на сервере
2. **Синхронизация файлов** - копируются все измененные файлы
3. **Остановка контейнеров** - останавливаются старые контейнеры
4. **Создание админа** - проверяется/создается администратор
5. **Сборка и запуск** - собираются образы и запускаются контейнеры
6. **Проверка** - проверяется работоспособность всех сервисов

## После деплоя

Приложение будет доступно по адресам:
- **Frontend**: http://185.247.17.188
- **Backend API**: http://185.247.17.188:8000
- **API Docs**: http://185.247.17.188:8000/docs

### Учетные данные по умолчанию:
- **Username**: `admin`
- **Password**: `admin`

⚠️ **Важно**: Смените пароль администратора после первого входа!

## Устранение проблем

### Проблема: sshpass не найден
```bash
# macOS
brew install hudochenkov/sshpass/sshpass

# Linux
sudo apt-get install sshpass
```

### Проблема: Ошибка подключения SSH
- Проверьте правильность адреса сервера и пароля
- Убедитесь, что SSH доступен на сервере
- Проверьте файрвол

### Проблема: Файлы не синхронизируются
- Проверьте правильность путей `LOCAL_PATH` и `SERVER_PATH`
- Убедитесь, что файлы существуют локально
- Проверьте права доступа на сервере

### Проблема: Контейнеры не запускаются
```bash
# Подключитесь к серверу и проверьте логи
ssh root@185.247.17.188
cd /root/agb_passports
docker-compose logs -f
```

### Проблема: Frontend не работает
- Убедитесь, что файл `frontend/lib/api.ts` синхронизирован
- Проверьте логи frontend: `docker logs agb_frontend`
- Пересоберите frontend: `docker-compose build frontend`

## Полезные команды

### На локальной машине:
```bash
# Полный деплой
./deploy_full.sh

# Быстрый деплой
./deploy_to_server.sh

# Синхронизация только файлов (без перезапуска)
./sync_all_to_server.sh
```

### На сервере (через SSH):
```bash
# Просмотр логов
docker-compose logs -f

# Статус контейнеров
docker-compose ps

# Перезапуск всех сервисов
docker-compose restart

# Перезапуск конкретного сервиса
docker-compose restart backend
docker-compose restart frontend

# Остановка всех сервисов
docker-compose down

# Пересборка и запуск
docker-compose up -d --build

# Проверка работоспособности
./verify_deployment.sh
```

## Автоматизация

Для автоматического деплоя при каждом коммите можно настроить Git hooks или CI/CD:

```bash
# Добавить в .git/hooks/post-commit
#!/bin/bash
./deploy_full.sh
```

## Безопасность

⚠️ **Важно**: 
- Не храните пароли в скриптах в production
- Используйте SSH ключи вместо паролей
- Ограничьте доступ к скриптам: `chmod 600 deploy_full.sh`
- Регулярно обновляйте пароли и ключи

## Поддержка

При возникновении проблем:
1. Проверьте логи: `docker-compose logs`
2. Проверьте статус контейнеров: `docker-compose ps`
3. Проверьте доступность сервисов: `./verify_deployment.sh`
4. Убедитесь, что все файлы синхронизированы

---

**Удачного деплоя! 🎉**
