# 🎨 Настройка логотипа для паспортов и наклеек

## Расположение логотипа

Логотип должен находиться в файле:
```
backend/utils/templates/logo.png
```

## Что было сделано

1. ✅ Обновлена функция `create_logo_image()` в `backend/utils/pdf_generator.py`
   - Теперь приоритет отдается `backend/utils/templates/logo.png`
   - Функция автоматически ищет логотип в правильном месте

2. ✅ Обновлен `sticker_template_generator.py`
   - Использует логотип из `backend/utils/templates/logo.png`
   - Правильно встраивает логотип в DOCX наклейки

3. ✅ Обновлен `docker-compose.yml`
   - Директория `backend/utils/templates` монтируется в контейнер
   - Логотип доступен по пути `/app/backend/utils/templates/logo.png` в контейнере

## Использование логотипа

Логотип автоматически используется в:
- ✅ **Паспортах (PDF)** - размер 18мм × 5.4мм
- ✅ **Наклейках (PDF)** - размер 18мм × 5.4мм  
- ✅ **Наклейках (DOCX)** - размер 18мм × 5.4мм

## Проверка работы

После деплоя логотип должен автоматически появляться в:
1. PDF паспортах при экспорте
2. PDF наклейках при экспорте
3. DOCX наклейках при экспорте

## Размеры логотипа

Рекомендуемый размер логотипа:
- **Ширина**: 18мм
- **Высота**: 5.4мм
- **Формат**: PNG
- **Разрешение**: достаточно для печати (300 DPI)

## Обновление логотипа

Для обновления логотипа:
1. Замените файл `backend/utils/templates/logo.png`
2. Запустите деплой: `./deploy_logo.sh`
3. Или скопируйте вручную на сервер и перезапустите backend

## Пути к логотипу

Приоритет поиска логотипа:
1. `/app/backend/utils/templates/logo.png` (в контейнере)
2. `/app/templates/logo.png` (fallback)
3. `backend/utils/logo.png` (fallback)
4. Автоматическое создание, если не найден

---

**Логотип настроен и готов к использованию! 🎉**
